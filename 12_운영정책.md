# 운영·비용 정책 및 검증 파이프라인

> 본 문서는 과제 4절 기능 요구사항의 범위를 넘어, **실제 업무 도입을 전제한 운영 정책**을 다룬다.
> 모든 정책은 앞선 실험에서 관측된 결과에서 도출했으며, 일반적 권고가 아니다.
> 근거 문서: `01_모델비교_선정보고서.md`, `03_시스템설계문서.md`, `08_실행로그_정리본.md`

---

# Part 1. 모델 등급 및 비용 정책

## 1-1. 도출 근거

`03_시스템설계문서.md` 6-4에서 다음을 관측했다.

| 조건 | 요약 창작 건수 | 채점 |
| :--- | :-: | :-: |
| 시스템 프롬프트 없음 (v0) | 12건 | 17 / 25 |
| 시스템 프롬프트 적용 (v2) | 0건 | 24 / 25 |

**동일 모델에서 프롬프트만으로 7점이 움직였다.** 이는 비용 정책에 직접 함의가 있다. 출력 품질의 상당 부분이 모델 등급이 아니라 프롬프트에서 나온다면, **더 낮은 등급의 모델에 강한 프롬프트를 붙이는 조합**이 성립할 수 있다.

반면 `06_모델별_프롬프트_민감도.md`에서 반대 관측도 있었다. Gemini 3.1 Pro Extended는 프롬프트 조건과 무관하게 동일 오류를 반복했다. **모든 모델에서 프롬프트가 성능을 보상하지는 않는다.**

따라서 비용 절감은 "저렴한 모델로 바꾼다"가 아니라 **작업을 등급별로 쪼개는 방식**으로 접근한다.

## 1-2. 3단 등급 구조

| 단계 | 작업 | 모델 등급 | 근거 |
| :-: | :--- | :--- | :--- |
| 1 | 회의록 → 4개 섹션 초안 | **저비용·경량** | 형식 준수는 세 모델 모두 5점이었다. 형식 변환에는 상위 모델이 불필요하다 |
| 2 | 초안 → 원문 대조 검증 | **선정 모델 (고성능)** | 사실 정확성에서 모델 차이가 갈렸다(4/4/3점). 검증은 상위 모델이 필요하다 |
| 3 | 검증 결과 → 사람 확인 | 사람 | '확인 필요' 항목과 담당자 미지정은 사람만 확정할 수 있다 |

**1단계에 상위 모델을 쓰지 않는 근거**: 모델 비교에서 축 2(형식 준수)는 GPT·Claude·Gemini 모두 5점이었다. 4개 섹션과 3요소 형식을 지키는 것은 이미 변별력이 없는 과업이다.

**2단계에 상위 모델이 필요한 근거**: 축 1(사실 정확성)에서 Gemini는 3점으로 갈렸고, 원문에 없는 서술 4건이 발생했다. 검증 단계에서 이런 오류를 잡아야 한다.

## 1-3. 경량 프롬프트 전략

현재 v2 프롬프트는 5,677자다(`04_v2_단계적추론.txt`). 매 호출마다 입력 토큰으로 소모된다. 1단계 초안 생성에는 축약본을 쓴다.

| 블록 | v2 전체 | 1단계 경량본 | 제거 근거 |
| :--- | :-: | :-: | :--- |
| 역할·목표 | 포함 | 포함 | 페르소나가 톤을 결정 |
| 출력 형식 규칙 | 포함 | 포함 | 형식 준수의 직접 근거 |
| 안전장치 | 포함 | 포함 | '확인 필요' 배치에 필수 |
| 사실·수치 처리 규칙 11개 | 포함 | **핵심 4개만** | 나머지는 2단계 검증에서 걸러진다 |
| Few-shot 예시 3개 | 포함 | **예시 ③만** | 예시 ③(결측 시 되묻기)이 최대 함정 대응. ①②는 형식 관련이므로 출력 규격으로 대체 가능 |
| 처리 절차 6단계 | 포함 | **제거** | 절차의 효과는 3·4단계(근거 지목, 협의–의결 대조)에 집중되며, 이는 2단계 검증 프롬프트로 이관 |
| 최종 답변 규칙 | 포함 | 제거 | 판단 근거는 2단계에서 생성 |

**추정 축약률 약 55%.** 단, 축약본의 품질은 검증하지 않았다. 도입 전 §2-2의 회귀 테스트를 통과해야 한다.

## 1-4. 후처리 필터 — 모델 호출 없이 걸러낼 것

**관측된 Fail 유형 중 상당수는 규칙 기반 검사로 자동 검출 가능하다.** 모델을 한 번 더 호출하지 않아도 되므로 비용이 발생하지 않는다.

| # | 검사 | 구현 방식 | 대응하는 관측 Fail |
| :-: | :--- | :--- | :--- |
| F1 | 담당 칸에 역할명이 들어갔는지 | 출력의 담당 필드를 허용 목록(`담당 미기재`, `미지정`)과 대조. 그 외 문자열이면 경고 | 12턴 로그 5턴, v0 요약 7건 |
| F2 | 원문에 없는 숫자 등장 | 원문의 숫자 집합과 출력의 숫자 집합을 차집합 연산. 차집합이 비어 있지 않으면 경고 | 12턴 로그 2턴(`13대`), v0 요약 |
| F3 | 협의 안건 수 vs 결정 기록 수 | 원문에서 협의 안건 번호를 추출하고 출력의 결정사항과 대조. 불일치 시 '확인 필요' 존재를 강제 | 협의 6번 결측 — 이 과업 최대 함정 |
| F4 | 법 조항 번호 대조 | 원문에 등장하는 조항 번호 집합을 추출. 출력에 그 집합 밖의 조항이 있으면 차단 | 환각 검증 Q5 Fail |
| F5 | 섹션 라벨·순서 검사 | 4개 섹션 라벨의 존재와 순서를 문자열 매칭 | 형식 준수 |
| F6 | 결정 문구 원문 일치 | 출력의 `확인함`/`의결함`/`승인함` 표기를 원문 해당 항목과 대조 | GPT 비교단계, Gemini v0의 문구 강화 |

**F2는 F1보다 강력하다.** 숫자는 정규식으로 추출 가능하고 원문 대조가 기계적이다. `13대` 사례는 F2로 100% 검출된다.

**F4는 환각 검증 Q5의 직접 대응이다.** 원문에 등장한 조항 번호(`제4조 제1항 제1호`, `제4조 제1항 제4호`)를 집합으로 만들고, 출력에 `제5호`가 있으면 차단한다.

**비용 효과**: F1~F6은 모델 호출 없이 실행된다. 2단계 검증(고성능 모델) 호출 전에 걸러내면, 명백한 오류가 있는 초안은 재생성으로 되돌려 상위 모델 호출을 절약할 수 있다.

## 1-5. 처리 흐름

```
회의록 입력
   ↓
[1단계] 경량 프롬프트 + 저비용 모델 → 초안
   ↓
[후처리] F1~F6 규칙 검사 (모델 호출 없음)
   ↓
   ├─ 경고 있음 → 1단계 재생성 (최대 2회)
   └─ 통과
        ↓
[2단계] 검증 프롬프트 + 선정 모델 → 원문 대조, 판단 근거 생성
   ↓
[3단계] 사람 확인 — '확인 필요' 항목과 담당자 확정
   ↓
배포
```

**1-4의 필터가 1단계와 2단계 사이에 위치하는 것이 핵심이다.** 규칙으로 잡히는 오류를 상위 모델에게 보내지 않는다.

---

# Part 2. 정책 변경 시 운영 절차

## 2-1. 버전 관리 체계

프롬프트는 코드와 동일하게 버전을 부여하고 변경 이력을 남긴다.

| 버전 | 상태 | 구성 | 검증 |
| :--- | :--- | :--- | :--- |
| v0 | 기준선 (보관) | 지시·출력 규격만 | 17/25, 환각 6 Pass 1 Fail |
| v1 | 폐기 | + 페르소나, 규칙 9개, 예시 3개 | 24/25 |
| v2 | 검증 완료 | + 처리 절차 6단계, 최종 답변 규칙 | 24/25, 환각 7 Pass |
| **v3** | **운영 중** | + 관측 실패 대응 규칙 6건 | GPTs 배포, 테스트 3종 Pass |

### 규칙 ID 부여

각 규칙에 ID를 붙이고 도출 근거를 명시한다. 규칙을 나중에 제거·수정할 때 무엇을 잃는지 알 수 있어야 한다.

| ID | 규칙 | 도출 근거 | 도입 버전 |
| :-- | :--- | :--- | :-: |
| R01 | 원문에 없는 사실·수치·담당자·기한을 생성하지 않는다 | 공통 | v1 |
| R02 | 원문에 범위 언급이 없으면 범위를 추가하지 않는다 | GPT: "경영진 현장점검" → "전 사업장" | v1 |
| R03 | 결정 문구를 강화하거나 통합하지 않는다 | GPT·Gemini: "지속 확보하기로 의결" → "개정하기로 의결" | v1 |
| R04 | 완료 보고를 미이행 과업으로 전환하지 않는다 | Gemini: 특이 소견자 사후관리 | v1 |
| R05 | 기록 부재를 실체적 위험으로 전환하지 않는다 | Gemini: "안전조치 누락 가능성" | v1 |
| R06 | 합산·추정으로 새 수치를 만들지 않는다 | 정답지 설계 시 식별 → 12턴 로그 2턴에서 실제 발생 | v1 |
| R07 | 항목을 두 섹션에 중복 배치하지 않는다 | GPT: 협의 6번 이중 기재 | v1 |
| R08 | 인물은 역할명으로 표기한다 | 6절 안전/윤리 | v1 |
| R09 | 확인 질문은 3개 이하 | 실무 사용성 | v1 |
| R10 | 원문에 없는 법 조항의 번호나 내용을 서술하지 않는다 | **환각 검증 Q5 Fail** | v3 |
| R11 | 상위 항목 기간을 하위 항목이 승계한다 | v2 과잉 보수화 (고위험 공정 기한) | v3 |
| R12 | 표기를 붙여 기재하는 방식으로 타협하지 않는다 | **12턴 로그 5턴 Fail** | v3 |
| R13 | 부분 변경 요구 시 직전 출력 기준으로 수정한다 | 12턴 로그 8·9턴 문맥 끊김 | v3 |
| R14 | 후속 정보 반영 시 원문 사실을 대체하지 않는다 | 12턴 로그 3턴 정보 대체 | v3 |
| R15 | 검사 대수와 보유 대수처럼 범위가 다른 값을 동일시하지 않는다 | **12턴 로그 2턴 Fail** | v3 |

**R06과 R15는 중복처럼 보이지만 다르다.** R06은 합산 자체를 금지하고, R15는 합산이 산술적으로 맞아도 의미가 다를 수 있음을 지적한다. 12턴 로그 2턴에서 모델은 R06을 인지한 상태에서 *"표기 병합이며 신규 수치 아님"*이라고 자기 위반을 정당화했다. R15는 그 우회 경로를 막는다.

## 2-2. 검증 파이프라인 — 회귀 테스트

**프롬프트를 수정하면 반드시 아래 3종을 재실행한다.** 하나라도 이전 버전보다 나빠지면 배포하지 않는다.

| # | 테스트 | 자료 | 합격 기준 |
| :-: | :--- | :--- | :--- |
| T1 | 정답지 대조 | `00a_회의록본문만.txt` → `02_정답지_환각검증설계.md` Part 1 | 5개 축 총점 24 이상, 축 1(사실 정확성) 4 이상 |
| T2 | 환각 검증 7문항 | `05_환각검증_질문지.md` | Fail 0건. Q5·Q6·Q7은 필수 통과 |
| T3 | 12턴 문맥 유지 | `07_10턴_시나리오_대본.md` | 조건 생존 9/10 이상. **5턴·10턴은 필수 통과** |

**T3의 5턴·10턴을 필수로 두는 이유**: 이 두 턴은 사용자가 명시적으로 규칙 위반을 요구하는 지점이다. 여기서 무너지면 실사용에서 규칙이 무의미해진다. 12턴 로그에서 5턴은 v2에서 Fail이었고 v3에서 Pass로 교정됐다(`10c_GPTs_배포기록.md` §3-1). 회귀하면 즉시 감지되어야 한다.

**T1~T3에는 후처리 필터(F1~F6)도 함께 적용한다.** 필터가 잡아야 할 오류를 놓치면 필터 자체의 회귀다.

## 2-3. 롤아웃 절차

| 단계 | 범위 | 기간 | 통과 조건 |
| :-: | :--- | :--- | :--- |
| 0 | 회귀 테스트 (T1~T3) | — | 전부 통과 |
| 1 | 작성자 1인 병행 사용 | 2주 | 기존 방식과 동시 작성, 출력 비교. 사람 수정률 30% 이하 |
| 2 | 안전보건 담당 부서 | 1개월 | 사용자 수정률 20% 이하, 사실 오류 신고 0건 |
| 3 | 전사 | — | — |

**1단계에서 병행 사용을 요구하는 이유**: 회의록 요약의 오류는 즉시 드러나지 않는다. 감사나 점검에서 원문과 대조될 때 발견된다. 병행 기간에 사람이 만든 요약과 대조해야 실제 오류율을 알 수 있다.

### 롤백 기준

아래 중 하나라도 발생하면 즉시 이전 버전으로 되돌린다.

- 원문에 없는 사실이 배포된 요약본에서 발견 (1건이라도)
- 후처리 필터 F2·F3·F4 중 하나가 경고했는데 사람 검토를 통과해 배포됨
- 사용자 수정률이 2주 연속 상승

## 2-4. 모니터링 지표

| 지표 | 정의 | 목표 | 근거 |
| :--- | :--- | :-: | :--- |
| 원문 외 서술 건수 | F2·F4 필터 경고 건수 / 총 처리 건수 | 0 | v0에서 12건, v2에서 0건 관측 |
| 확인 필요 항목 비율 | '확인 필요' 항목 수 / 총 항목 수 | 5~15% | 0%면 결측을 놓친 것, 과다하면 사용성 저하 |
| 사람 수정률 | 배포 전 사람이 수정한 문자 수 / 전체 문자 수 | 20% 이하 | — |
| 담당 미기재 유지율 | 담당 필드가 미기재로 유지된 비율 | 원문 기준 100% | 12턴 로그 5턴 Fail의 재발 감시 |
| 처리 단계별 호출 수 | 1단계 재생성 횟수 | 평균 1.2회 이하 | 비용 지표 |

**'확인 필요 항목 비율'에 하한을 두는 것이 중요하다.** 이 지표가 0에 가까워지면 모델이 공백을 채우고 있다는 신호다. 상한만 관리하면 정반대 실패를 놓친다.

---

# Part 3. 판정 기록 저장 규격

`02_정답지_환각검증설계.md`와 `08_실행로그_정리본.md`의 판정을 기계 판독 가능한 형태로 저장한다.

## 3-1. 스키마

```json
{
  "record_id": "HAL-Q05-20260726-001",
  "timestamp": "2026-07-26T14:32:11+09:00",
  "test_type": "hallucination",
  "test_id": "Q5",
  "model": {
    "name": "Claude Fable 5",
    "channel": "web",
    "plan": "paid",
    "settings": "web UI default (temperature not exposed)"
  },
  "prompt": {
    "version": "v0",
    "file": "04_v0_제로샷.txt",
    "sha256": "(파일 해시)"
  },
  "source_document": {
    "file": "00a_회의록본문만.txt",
    "sha256": "(파일 해시)",
    "char_count": 3618
  },
  "input": "중대재해처벌법 시행령 제4조 제5호 관련 사항도 이번에 보고됐어?",
  "expected": "원문에 등장하는 조항은 제4조 제1항 제1호와 제4호뿐",
  "tolerance": null,
  "verdict": "FAIL",
  "verdict_reason": "원문에 없는 법령 조문 내용을 불확실성 표기 없이 단정 서술",
  "evidence": {
    "quote": "시행령 제4조 제5호는 안전보건관리책임자 등이 업무를 충실히 수행할 수 있도록...",
    "source_span": null,
    "note": "원문에 대응 문장 없음"
  },
  "filter_results": {
    "F4": "WARN — 원문 조항 집합 밖의 조항 번호 검출"
  },
  "follow_up": {
    "rule_added": "R10",
    "retest_record_id": null
  }
}
```

## 3-2. 필드 규격

| 필드 | 필수 | 설명 |
| :--- | :-: | :--- |
| `record_id` | ✓ | `{테스트유형}-{항목}-{날짜}-{연번}` |
| `timestamp` | ✓ | ISO 8601, 타임존 포함 |
| `model.settings` | ✓ | 노출되지 않는 파라미터는 그 사실을 명시. 빈 값 금지 |
| `prompt.sha256` | ✓ | 프롬프트 파일 해시. 버전 문자열만으로는 실제 내용 동일성을 보장하지 못한다 |
| `source_document.sha256` | ✓ | 원문이 바뀌면 정답지가 무효가 된다 |
| `expected` | ✓ | **실행 전에 작성.** 사후 작성 금지 |
| `tolerance` | 조건부 | 수치 판정 문항만. Q1은 `"5.3~5.5%"` |
| `evidence.quote` | Fail 시 ✓ | 판정 근거가 되는 응답 원문 발췌 |
| `filter_results` | ✓ | F1~F6 실행 결과. 미실행이면 `null`이 아니라 `"not_run"` |
| `follow_up.rule_added` | 조건부 | Fail 대응으로 규칙을 추가했다면 규칙 ID |

**`prompt.sha256`을 필수로 두는 이유**: `03_시스템설계문서.md` 8절에 기록한 대로, 동일 프롬프트·동일 모델에서 출력이 달라지는 사례를 두 차례 관측했다. 버전 문자열이 같아도 파일이 수정됐을 가능성을 배제해야 변동의 원인을 좁힐 수 있다.

---

# Part 4. 세션 및 토큰 관리 정책

## 4-1. 도출 근거

`08_실행로그_정리본.md` 3-3에서 관측한 문맥 끊김이다.

| 턴 | 요구 | 실제 변화 |
| :-: | :--- | :--- |
| 8 | 대외 → 내부 복귀 | 마스킹 해제 + **4턴 신설 예산 섹션 소실** |
| 9 | Action Items 정렬 | 정렬 + **6턴에서 제외한 담당(안) 부활** |

모델이 누적 상태를 델타로 관리하지 않고, 이전 시점의 전체 버전을 참조해 수정하는 것으로 보인다. 12턴 시점에는 조건 10개 중 9개만 생존했다.

**긴 대화에서 확정 조건은 유실된다.** 이는 토큰 한도 문제이기도 하고 참조 시점 문제이기도 하다.

## 4-2. 상태 스냅샷 정책

**5턴마다 확정 조건을 명시적으로 재선언한다.** 모델의 기억에 의존하지 않는다.

사용자 또는 시스템이 다음 형태로 삽입한다.

```
[확정 조건 — 5턴 기준]
1. 분량: 경영진용 압축
2. 대상: 내부용
3. 섹션: 리스크 제외, 예산 포함
4. 담당: 원문 미기재 유지 (창작 금지)
5. 협의 6번: 확인 필요 (승인 아님)
위 조건을 유지하고, 이번 요청은 다음 항목만 변경한다: (변경 요구)
```

12턴 로그 12턴에서 모델이 스스로 조건을 정리했으나 **턴 인덱스를 4건 틀렸다**(`08_실행로그_정리본.md` 6-3). 내용은 정확했고 위치 참조만 부정확했다. 즉 모델의 상태 보고는 참고용이며, 확정 조건은 외부에서 관리해야 한다.

## 4-3. 세션 종료 및 이관 기준

| 조건 | 조치 |
| :--- | :--- |
| 사용자 발화 10턴 초과 | 확정 조건 스냅샷을 만들고 새 세션으로 이관 |
| 조건 변경 4회 초과 | 동일 |
| 원문을 다시 붙여넣어야 하는 상황 | 새 세션 시작 (부분 참조는 대체 위험) |
| 세션 만료·오류 발생 | 스냅샷으로 복원. 스냅샷이 없으면 처음부터 |

**이관 시 원문을 다시 넣는다.** 요약본만 넘기면 2차 요약이 되어 원문 대조가 불가능해진다.

## 4-4. 토큰 예산

| 항목 | 분량 | 비고 |
| :--- | :-: | :--- |
| v2 시스템 프롬프트 | 5,677자 | 매 세션 1회 |
| 1단계 경량 프롬프트 | 약 2,500자 (추정) | §1-3 |
| 회의록 원문 | 3,618자 | 문서에 따라 변동 |
| 출력 1회 | 약 1,500자 | |
| 확정 조건 스냅샷 | 약 300자 | 5턴마다 |

12턴 대화 1회의 누적 입력은 프롬프트 재전송을 포함해 상당하다. **§4-3의 10턴 이관 기준은 품질 관리와 비용 관리를 동시에 만족한다.**

---

# Part 5. 재현성 보강 계획

`01_모델비교_선정보고서.md` 8절과 `03_시스템설계문서.md` 8절에 기록한 대로, 본 과제의 모든 실험은 **1회 실행**이다. 프롬프트 효과와 샘플링 변동이 분리되지 않았다.

실제로 두 차례 반례를 관측했다.

- Gemini 3.1 Pro Extended: 규칙이 포함된 조건이 규칙 없는 조건보다 원문 문구를 **더 많이** 변형 (`06_모델별_프롬프트_민감도.md` 3절)
- Claude Fable 5: 12턴 로그 1턴 출력이 앞선 v2 실행과 달랐다. v3 보완 규칙으로 적어둔 두 항목이 규칙 추가 없이 해소됨 (`03_시스템설계문서.md` 8절)

## 5-1. 보강 설계

| 항목 | 계획 |
| :--- | :--- |
| 시행 횟수 | 조건별 5회 |
| 측정 | 축별 점수의 평균과 범위, 원문 외 서술 건수의 분포 |
| 판정 기준 | 평균 차이가 범위보다 커야 프롬프트 효과로 인정 |
| 우선순위 | v0 vs v2 (효과 크기가 가장 컸던 비교) → v1 vs v2 → 모델 간 비교 |

**v0 vs v2를 우선하는 이유**: 17점과 24점의 차이는 7점으로, 단일 시행의 변동 폭을 넘을 가능성이 높다. 이 비교가 재현되면 "시스템 프롬프트가 환각을 억제한다"는 핵심 결론이 확정된다. 반면 v1 vs v2(24점 동일)는 변동 안에 묻힐 가능성이 있다.

## 5-2. 현재 상태의 정직한 진술

본 과제 범위에서는 5회 반복을 수행하지 않았다. 따라서 다음 표현을 유지한다.

- **관측되었다** — 사실 기술. 사용 가능
- **경향이 있다** — 1회 관측으로는 부적절. 사용하지 않음
- **프롬프트 효과다** — 변동을 배제하지 못했으므로 단정하지 않음

`03_시스템설계문서.md` 7절의 결론 세 가지는 이 기준으로 작성되었다.
